meta {
  name: Record Lap Time
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/races/{{raceId}}/lap-times
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "kartNumber": 1,
    "timestamp": "2026-02-16T00:00:00"
  }
}

docs {
  Records a lap time for a specific kart in the race.

  The timestamp represents when the kart crossed the start/finish line.
  Call this endpoint multiple times to simulate a race with multiple lap completions.

  **Note**: Make sure to set the raceId environment variable from the Start Race response.
}

tests {
  test("Status code is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
